language: php

php:
  - 7.0
  - 7.1

sudo: true

install:
- travis_retry composer install --no-interaction --prefer-source

script:
- ./vendor/bin/phpunit --coverage-clover tests/logs/clover.xml

after_success:
- travis_retry php vendor/bin/coveralls -v -x tests/logs/clover.xml